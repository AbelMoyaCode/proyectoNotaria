<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para la API REST -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_Notaria"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".control.MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".control.LoginActivity"
            android:noHistory="false" />
        <activity
            android:name=".control.RegistroActivity"
            android:noHistory="false" />
        <activity
            android:name=".control.ListadoTramitesActivity"
            android:parentActivityName=".control.MainActivity"
            android:noHistory="false" />
        <activity
            android:name=".control.DetalleTramiteActivity"
            android:parentActivityName=".control.ListadoTramitesActivity"
            android:noHistory="false" />
        <activity
            android:name=".control.AgendarCitaActivity"
            android:parentActivityName=".control.DetalleTramiteActivity"
            android:noHistory="false" />
        <activity
            android:name=".control.ConfirmacionCitaActivity"
            android:parentActivityName=".control.AgendarCitaActivity"
            android:noHistory="false" />
    </application>

</manifest>